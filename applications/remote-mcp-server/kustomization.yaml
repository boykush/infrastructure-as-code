apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# Image Updater rewrites this block, pinning the digest behind the tag.
images:
  - name: ghcr.io/boykush/remote-mcp-server
    newTag: main
